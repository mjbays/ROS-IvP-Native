//--------------------------------------------------------
// uXMS Configuration Block (Created from Plugin)

ProcessConfig = uXMS
{
  AppTick   = 2
  CommsTick = 2
  
  VAR = NODE_REPORT
  VAR = APPCAST_REQ
  VAR = APPCAST
  VAR = PMB_REGISTER
  VAR = PMB_UDP_LISTEN

  DISPLAY_SOURCE        = true
  DISPLAY_TIME          = true
  DISPLAY_COMMUNITY     = false   // default

  REFRESH_MODE = streaming
}


